---

package_repo:
  state: "{{ package_repo_state|default('present') }}"

package_repo_is_enabled:
  rpmfusion:
    rpmfusion: true
    debuginfo: false
    source: false
package_repo_gpgcheck:
  rpmfusion:
