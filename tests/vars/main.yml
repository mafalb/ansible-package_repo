# vim: set ft=yaml ts=2:
---

package_repo:
  tests:
    epel:
      containers:
        - container_name: centos-7
          image_name: centos-7-image
          src: centos-7
          volumes:
          - /sys/fs/cgroup:/sys/fs/cgroup:ro
          member_of_groups:
            - testnodes
            - centos
            - epel
